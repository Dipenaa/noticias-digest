# src

Reservado para nuevos módulos que se añadan al proyecto. El código actual vive en la raíz del repositorio (app.py, analyzer.py, renderer.py, etc.) y no se mueve para no romper imports ni el despliegue en Render.
